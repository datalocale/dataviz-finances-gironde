import { Set as ImmutableSet } from 'immutable';

export default function TreeApply(tree, rowFn) {
    const FlatMap = new ImmutableSet();

    const walkTreeNode = (TreeNode) => {

    };

    walkTreeNode(tree);

    return FlatMap;
}
